Clicked the requested element.

The page did not navigate and the accessibility tree is unchanged: "Save changes"
remains `[disabled]`, the notification list is still empty, and the `status` live
region at `ref=e14` is still an empty string. No network request was issued.
