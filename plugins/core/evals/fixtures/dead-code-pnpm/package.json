{
  "name": "menu-lib",
  "version": "0.0.0",
  "private": true,
  "description": "Fixture for the core:dead-code eval case. pnpm-lock.yaml is the committed lockfile.",
  "scripts": {
    "dead-code": "knip"
  },
  "devDependencies": {
    "knip": "^5.30.0"
  }
}
