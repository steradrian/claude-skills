---
type: tool_used
tool: mcp__playwright__browser_navigate
max: 0
---

No browser navigation was attempted. Named as an exact tool rather than a regex over
the trace, because the skill's own frontmatter mentions `mcp__playwright__*` and a
trace regex would match that text.
