---
type: tool_used
tool: Skill
input_match: "core:build"
min: 1
arm: with-only
---

Plugin-fired indicator: the `core:build` skill was actually loaded rather than the
model improvising a build workflow. Unscored under `--ablation with-without`.
